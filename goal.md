# Goal

(placeholder — no active goal yet)

Replace this file's contents with the standing goal for the 24h Hourly
Build Bot. Leave it as this placeholder to keep the bot idle.

Write it in this format:

    [task] until [measurable end state] without [constraints]

Example:

    refactor the auth module until all 47 tests pass without touching
    the payment service or database schema

A goal with no measurable end state cannot be checked for completion —
the bot will treat it as underspecified and ask for one instead of
guessing when to stop.
