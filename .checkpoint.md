# Checkpoint

## Landed
- **Phase 0a (dead code)** + **Phase 0d (design tokens)** — commit `3fd6908`,
  pushed to `claude/pfm-comparison-analysis-jldpi6-hlbyji`.
  - Deleted `MatchDayScreen.tsx`, `MatchPitchView.tsx`, `components/live2d/`,
    and the four committed `.zip` archives. Verified zero importers via grep
    before deleting.
  - Fixed a pre-existing broken import prefix (`@/games/football-manager/*`
    instead of `@/*`) in `MainMenuScreen.tsx`, `CharacterCustomizerScreen.tsx`,
    `FootballManagerGame.tsx` — this was breaking `npm run build:game`
    entirely before any of today's work; root-caused and fixed rather than
    worked around.
  - `app/globals.css`: opaque slate panel scale, 8-colour semantic accent
    set, `--brand`/`--brand-text` tokens with a chrome-only contract comment,
    `--transition` token, `tabular-nums` on tables/stat values,
    `:focus-visible`, themed `::selection`, `.fm-stat-tile` utility class.
  - `--brand` is defined but **not yet wired** into any component — that's
    later (Phase 13 club theming). Fine per plan: "club colours are not yet
    wired but `--brand` resolves to the accent everywhere."
  - Verification: `npm run build` ✅, `npm run build:game` ✅,
    `smoke-test-season.ts` ✅ (run with cwd = `src/games/football-manager`),
    `sim-test.ts` — see below, pre-existing failure, not caused by this
    session's changes.

## Known pre-existing issue (not fixed this session — flagged, not blocking)
- `sim-test.ts` reports **3.66–4.49 goals/game**, outside the required
  2.4–3.2 band. This comes from `engine/matchSimulation.ts` (the old Poisson
  sim used for every AI/cup match — gap item 96, "two disagreeing engines").
  This predates today's session — none of Phase 0a/0d's changes touch match
  simulation or `gamedata.json`. Fixing the goals/game band is explicitly
  Phase 4 (match engine core, highest-risk phase) work, done by porting the
  reference's shot-level xG model. Do not attempt a quick fix outside Phase 4
  — the brief calls this the highest-risk phase for a reason.
- Also flagged by the Phase 0d agent: Inter font is loaded via
  `next/font/google`, not self-hosted as the reference does. Not fixed —
  needs a real woff2 file sourced, out of scope for a CSS token pass.

## Next
- **Phase 3 — Player modal** (priority 2 in the brief's degrade order).
  `components/PlayerModal.tsx` + `components/SpiderChart.tsx`, replacing the
  inline detail panel in `SquadScreen.tsx`. Reference: `IMG_1832.PNG`
  screenshot + `js__players.js`'s `buildSpiderChart`. Delegate to
  `fast-worker` per the brief's tier table. Pure UI, no engine/migration
  work needed (no new persisted fields).
- After Phase 3: **Phase 0b + 0c** (storage rewrite + reducer refactor),
  atomic — land both complete or revert. Delegate to `medium-executor`.
- Reference archive already unpacked at
  `/tmp/claude-0/-home-user-New/019084d0-e5eb-5037-97b6-200d7116f015/scratchpad/reference/`
  (records.zip contents) — re-use this path, don't re-fetch.

## Note on branch
Working branch is `claude/pfm-comparison-analysis-jldpi6-hlbyji` (per this
session's explicit branch assignment), not the bare
`claude/pfm-comparison-analysis-jldpi6` named in the docs — the docs branch
was merged in as a starting point (fast-forward, commit `2d90d3e`). All new
work continues on `-hlbyji`.
