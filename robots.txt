# robots.txt — BALLKNW crawling rules

# Everything is crawlable by default. The per-page Allow list that used to live
# here was hand-maintained and had fallen ~60 pages behind the site, which made
# it look like an allowlist rather than the no-op it actually was.
User-agent: *
Allow: /
Disallow: /footygames/
Disallow: /admin/
Disallow: /api/
# seo-plan-50.html is kept out of the index with a noindex meta tag rather than
# a Disallow here — blocking the crawl would stop Google reading that tag.

# AI crawlers — generous allowance for content indexing
User-agent: GPTBot
Allow: /

User-agent: CCBot
Allow: /

User-agent: PerplexityBot
Allow: /

User-agent: anthropic-ai
Allow: /

Sitemap: https://www.ballknw.com/sitemap.xml
