# Supabase — these publishable values are safe to expose to the browser.
# Copy this file to .env.local for local dev, and set the same vars in Vercel.
NEXT_PUBLIC_SUPABASE_URL=https://okghgvobjdysqxslibab.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_dT7dQqMP_h5OLF3pe78NbA_3wzCo4mo

# Service-role key — SERVER ONLY. Never expose to the browser or commit it.
# Needed only to run the one-time data import script (npm run import-data) and
# the schema migration. Grab it from Supabase Dashboard → Project Settings → API.
SUPABASE_SERVICE_ROLE_KEY=
